tremor_losses:
  # Loss component weights
  bce_weight: 1.0
  focal_weight: 0.0
  tversky_weight: 0.0
  # Class imbalance correction (for 77% PD, 23% Healthy)
  healthy_weight: 1.0     
  parkinson_weight: 1.0


drawing_losses:
  # Loss component weights
  bce_weight: 1.0
  focal_weight: 0.0
  tversky_weight: 0.0
  # Positive-class (PD) weighting
  pos_weight: 1.5


questionnaire_losses:
  # Loss component weights
  bce_weight: 1.0
  focal_weight: 0.0
  tversky_weight: 0.0
  # Label smoothing
  label_smoothing: 0.03
  # Class imbalance correction
  healthy_weight: 1.5
  parkinson_weight: 1.0


audio_losses:
  # Loss component weights
  bce_weight: 1.0
  focal_weight: 0.0
  tversky_weight: 0.0
  # Label smoothing
  label_smoothing: 0.03
  # Class imbalance correction
  healthy_weight: 1.5
  parkinson_weight: 1.0
